local wezterm = require("wezterm")

local config = {
  font = wezterm.font("JetBrains Mono"),
  -- color_scheme = "Batman",
  font_size = 9.0,
  window_background_opacity = 0.9,
  exit_behavior = "Close",
}

return config
